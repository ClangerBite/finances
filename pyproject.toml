[project]
name = "finances"
version = "0.1.0"
description = "Analysis of personal finances"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.2",
    "numpy>=2.3.2",
    "pyyaml>=6.0.2",
]
